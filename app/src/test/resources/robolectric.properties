# targetSdk is 36; Robolectric 4.15 ships android-all jars up to 35. Pin the
# default so tests without an explicit @Config still run. Version-sensitive
# tests declare their own @Config(sdk = [...]) range.
sdk=34
